server:
  port: 15678  # HTTP (Tomcat) port

spring:
  application:
    name: ts-verification-code-service
  cloud:
    config:
      uri: http://ts-config-service:15679
  data:
    mongodb:
      host: ts-verification-code-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017

opentracing:
  jaeger:
    udp-sender:
      host: jaeger
      port: 6831

