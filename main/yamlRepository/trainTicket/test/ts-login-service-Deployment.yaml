apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-login-service
  namespace: train-ticket
  labels:
    app: ts-login-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-login-service
      version: v1
  template:
    metadata:
      labels:
        app: ts-login-service
        version: v1
    spec:
      containers:
      - name: ts-login-service
        image: ksh6283/ts-login-service:v6
        ports:
        - containerPort: 12342
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 200m
            memory: 512Mi