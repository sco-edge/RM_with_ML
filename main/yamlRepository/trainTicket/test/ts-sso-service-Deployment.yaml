apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-sso-service
  namespace: train-ticket
  labels:
    app: ts-sso-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-sso-service
      version: v1
  template:
    metadata:
      labels:
        app: ts-sso-service
        version: v1
    spec:
      containers:
      - name: ts-sso-service
        image: ksh6283/ts-sso-service:v7
        ports:
        - containerPort: 12349
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 200m
            memory: 512Mi