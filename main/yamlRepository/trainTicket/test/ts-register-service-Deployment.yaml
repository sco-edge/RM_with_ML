apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-register-service
  namespace: train-ticket
  labels:
    app: ts-register-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-register-service
      version: v1
  template:
    metadata:
      labels:
        app: ts-register-service
        version: v1
    spec:
      containers:
      - name: ts-register-service
        image: ksh6283/ts-register-service:v4
        ports:
        - containerPort: 12345
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 200m
            memory: 512Mi