apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-consign-service
  labels:
    app: ts-consign-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-consign-service
      version: v1
  template:
    metadata:
      labels:
        app: ts-consign-service
        version: v1
    spec:
      containers:
      - name: ts-consign-service
        image: ksh6283/ts-consign-service:v2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 16111
      tolerations:
      - key: "node-role.kubernetes.io/control-plane"
        operator: "Exists"
        effect: "NoSchedule"