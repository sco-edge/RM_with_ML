apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-contacts-service
  namespace: train-ticket
  labels:
    app: ts-contacts-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-contacts-service
      version: v1
  template:
    metadata:
      labels:
        app: ts-contacts-service
        version: v1
    spec:
      containers:
      - name: ts-contacts-service
        image: ksh6283/ts-contacts-service:v5
        ports:
        - containerPort: 12347
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 200m
            memory: 512Mi